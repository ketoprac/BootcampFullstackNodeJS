const numb = [1, 2, 3, 4, 5, 6];
const multiple = numb.map(e => e*5);
console.log(multiple)

const code = ['Java', 'Javascript', 'Python', 'SQL', 'Oracle'];
const db = code.filter(e => e.startsWith('Jav'));
console.log(db)